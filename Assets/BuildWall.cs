using UnityEngine;
using VRTK;

public class BuildWall  {
/*
    public PrimitiveTypes shape;
    public bool resized = false;

    public override void StartUsing(VRTK_InteractUse usingObject)
    {
        base.StartUsing(usingObject);

        if(!resized)
        {
            transform.localScale += new Vector3(0, 0.2F, 15F);
            resized = true;
        } else {
            transform.localScale += new Vector3(0, -0.2F, -15F);
            resized = false;
        }
        ResetMenuItems();
    }

    private void ResetMenuItems()
    {
        foreach (Menu_Object_Spawner menuObjectSpawner in FindObjectsOfType<Menu_Object_Spawner>())
        {
            menuObjectSpawner.StopUsing();
        }
    }
    */
}
